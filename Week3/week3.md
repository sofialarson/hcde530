# Competency 2 — Code Literacy and Documentation (Week 3)

**Course definition:** *“Code Literacy and Documentation. Reading code well enough to explain what it does, change it when needed, and document it so someone else -- or future you -- can follow it. This icludes inine comments, docstrings, commit messages, and README content.”*

## Claim
For this week, I used a python script to read through survey data and generate a CSV file based on the responses that would provide summary statistics on the top satsification scores, the number of responses by role, the average years of experience by respondents, and a list of products that were mentioned to have insights via the survey. The initial script was buggy, and produced two errors that were addressed via commented commits, which are listed below.

1. With the first bug, this occurred initially on Line 30 of the week3_analysis.py script, and then was addressed via the introduction of experience_text.isdigit(): at line 62. Initially, when the script was run with this bug, it presented an error stating ValueError: invalid literal for int(). This was because one survey response had presented their response to a question about years of experience in the form of a string, rather than a number. This revealed there was a mix of string and integer data. Now, I added additional code to skip over any responses that aren't an integer for experience. This was highlighted via a commit. 

2. For the second bug, this initially occured on Line 45 of the script. Though no error was presented by the terminal, the report generated by the script had a section meant to show top satisifaction scores in descending order, from highest to lowest. However, instead, it was in ascending order. To address this, I added additioknal code to revise the sorting function to be in descending order. Now, the scores generate from highest scores to lower scores. This change was highlighted via a commit message. 
